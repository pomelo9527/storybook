name: storybook_ai_full
description: Full AI Storybook Reader
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  flutter_tts: ^4.0.2
  pdfx: ^2.6.0
  page_flip: ^0.2.1
  file_picker: ^8.0.0
  google_mlkit_text_recognition: ^0.11.0
  http: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true